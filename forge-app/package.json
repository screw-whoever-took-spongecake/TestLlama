{
  "name": "testllama-forge-app",
  "version": "1.0.0",
  "private": true,
  "description": "TestLlama Jira Forge integration – issue panel for managing linked test cases",
  "main": "src/resolvers/index.js",
  "scripts": {
    "build": "cd src/frontend && npm run build",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
