import type { ReactElement } from 'react';

export default function TestRuns(): ReactElement {
  return (
    <section className="page">
      <h2>Test Runs</h2>
      <p>View and create test runs. (No database connected.)</p>
    </section>
  );
}
